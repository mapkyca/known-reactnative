# Known IOS / Android App

This is a first stab at a Known IOS (and potentially Android) native installable application.

It is written using React Native and makes use of Known's in built API to work.

